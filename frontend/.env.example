# Optional override. By default the app uses /api (Vite proxy in dev).
VITE_API_BASE_URL=/api

# Optional frontend login credentials (defaults: admin / admin123)
VITE_LOGIN_USERNAME=admin
VITE_LOGIN_PASSWORD=admin123
